name: meep_porechop
channels:
  - conda-forge
  - bioconda
  - nodefaults
dependencies:
  # Porechop 0.2.4 is unmaintained and incompatible with python >=3.10
  - python >=3.9,<3.10
  - porechop >=0.2.4

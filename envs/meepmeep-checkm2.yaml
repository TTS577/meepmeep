name: meepmeep-checkm2
channels:
  - bioconda
  - conda-forge
  - nodefaults
dependencies:
  - python >=3.9,<3.13
  # checkm2 >=1.0.3 uses ONNX Runtime instead of tensorflow,
  # avoiding the grpcio/c-ares dependency conflict.
  - checkm2 >=1.0.3

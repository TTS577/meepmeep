name: meepmeep-checkm2
channels:
  - bioconda
  - conda-forge
  - defaults
dependencies:
  # checkm2 >=1.0.3 no longer requires tensorflow, avoiding the
  # grpcio/c-ares dependency conflict present in 1.0.2.
  - checkm2 >=1.0.3
